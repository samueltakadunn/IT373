#include <stdio.h>

int main ()
    {
        int num = 123;
        int sum = 0;
        sum = sum + num;
        sum = sum + num;
        sum = sum + num;
        sum = sum + num;
        printf("%d\n", sum);
        return 0;
    }